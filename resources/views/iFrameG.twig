{% set width  =       config("EasyFrame.frame.g.width") %}
{% set height =       config("EasyFrame.frame.g.height") %}
{% set url    =       config("EasyFrame.frame.g.url") %}

<iframe src="{{ url }}" height="{{ height }}" width="{{ width }}"></iframe>