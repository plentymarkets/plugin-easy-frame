{% set width  =       config("EasyFrame.frame.d.width") %}
{% set height =       config("EasyFrame.frame.d.height") %}
{% set url    =       config("EasyFrame.frame.d.url") %}

<iframe src="{{ url }}" height="{{ height }}" width="{{ width }}"></iframe>