{% set width  =       config("EasyFrame.frame.e.width") %}
{% set height =       config("EasyFrame.frame.e.height") %}
{% set url    =       config("EasyFrame.frame.e.url") %}

<iframe src="{{ url }}" height="{{ height }}" width="{{ width }}"></iframe>