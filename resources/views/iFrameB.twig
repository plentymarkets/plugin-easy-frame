{% set width  =       config("EasyFrame.frame.b.width") %}
{% set height =       config("EasyFrame.frame.b.height") %}
{% set url    =       config("EasyFrame.frame.b.url") %}

<iframe src="{{ url }}" height="{{ height }}" width="{{ width }}"></iframe>