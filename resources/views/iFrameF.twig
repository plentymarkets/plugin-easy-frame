{% set width  =       config("EasyFrame.frame.f.width") %}
{% set height =       config("EasyFrame.frame.f.height") %}
{% set url    =       config("EasyFrame.frame.f.url") %}

<iframe src="{{ url }}" height="{{ height }}" width="{{ width }}"></iframe>