{% set width  =       config("EasyFrame.frame.h.width") %}
{% set height =       config("EasyFrame.frame.h.height") %}
{% set url    =       config("EasyFrame.frame.h.url") %}

<iframe src="{{ url }}" height="{{ height }}" width="{{ width }}"></iframe>