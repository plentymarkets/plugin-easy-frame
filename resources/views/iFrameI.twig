{% set width  =       config("EasyFrame.frame.i.width") %}
{% set height =       config("EasyFrame.frame.i.height") %}
{% set url    =       config("EasyFrame.frame.i.url") %}

<iframe src="{{ url }}" height="{{ height }}" width="{{ width }}"></iframe>